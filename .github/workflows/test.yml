name: Manual Test

on:
  push:
    branches: [ Microservices ]
    paths:
      - 'SP.ConfigService/**'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from Microservices branch"
