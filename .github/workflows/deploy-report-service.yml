name: Deploy Report Service

on:
  push:
    branches: [ Microservices ]
    paths:
      - 'SP.ReportService/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./SP.ReportService/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/sp-report-service:latest

      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            docker pull ${{ secrets.DOCKER_USERNAME }}/sp-report-service:latest
            docker stop sp-report-service || true
            docker rm sp-report-service || true
            docker run -d --name sp-report-service -p 8398:80 ${{ secrets.DOCKER_USERNAME }}/sp-report-service:latest