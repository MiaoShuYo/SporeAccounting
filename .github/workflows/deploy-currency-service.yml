name: Deploy Currency Service

on:
  push:
    branches: [ Microservices ]
    paths:
      - 'SP.CurrencyService/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./SP.CurrencyService/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/sp-currency-service:latest

      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            docker pull ${{ secrets.DOCKER_USERNAME }}/sp-currency-service:latest
            docker stop sp-currency-service || true
            docker rm sp-currency-service || true
            docker run -d --name sp-currency-service -p 8398:80 ${{ secrets.DOCKER_USERNAME }}/sp-currency-service:latest 